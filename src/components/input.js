import React from 'react';

const Input = (title) => {
    return (
        <div className='input'> 
            <input />
            <label>{title}r</label>
        </div>
    )
}

export default Input;